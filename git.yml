---

- hosts: localhost
  tasks:

  - name: ensure git is installed
    become: yes
    package:
      name: git
      state: present

  - name: copy gitconfig to user home dir
    become: no
    copy:
      src: lib/vimrc
      dest: ~/.vimrc
      mode: '0664'
      backup: yes
